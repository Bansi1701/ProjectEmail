# Copy to .env.local. NEVER commit real values.

NEXT_PUBLIC_API_URL=http://localhost:8000
# Email HTML renders here — MUST differ from the app origin. See docs/SECURITY.md section 1.
NEXT_PUBLIC_SANDBOX_ORIGIN=http://localhost:8001

NEXT_PUBLIC_TURNSTILE_SITE_KEY=
NEXT_PUBLIC_SENTRY_DSN=

# Ad ops — Phase 3. No ad script may load before CMP consent.
NEXT_PUBLIC_GAM_NETWORK_CODE=
NEXT_PUBLIC_CMP_ID=
